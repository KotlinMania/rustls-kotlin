# Rust callers — `rustls-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `rustls` | `rustls` | 27 | 6 | 6 | 2 |

## `rustls`

### Inbound symbols (19 distinct, 27 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `rustls`.

#### `rustls::RootCertStore` — 5 site(s) (1 codex, 4 other, 5 caller crates)

- ✅ `codex-kotlin/codex-client` `src/custom_ca.rs:52` — `use rustls::RootCertStore`
- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::RootCertStore`
- `runfiles-kotlin/pkg_b` `src/lib.rs:1` — `use rustls::RootCertStore`
- `tokio-tungstenite-kotlin/tokio-tungstenite` `src/tls.rs:68` — `use rustls::RootCertStore`
- `tungstenite-kotlin/tungstenite` `src/tls.rs:73` — `use rustls::RootCertStore`

#### `rustls::ClientConfig` — 3 site(s) (1 codex, 2 other, 3 caller crates)

- ✅ `codex-kotlin/codex-client` `src/custom_ca.rs:51` — `use rustls::ClientConfig`
- `tokio-tungstenite-kotlin/tokio-tungstenite` `src/tls.rs:67` — `pub_use rustls::ClientConfig`
- `tungstenite-kotlin/tungstenite` `src/tls.rs:73` — `use rustls::ClientConfig`

#### `rustls::StreamOwned` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `tungstenite-kotlin/tungstenite` `src/stream.rs:19` — `use rustls::StreamOwned`
- `tungstenite-kotlin/tungstenite` `src/tls.rs:73` — `use rustls::StreamOwned`

#### `rustls::client::danger::ServerCertVerifier` — 2 site(s) (0 codex, 2 other, 2 caller crates)

- `rama-tls-rustls-kotlin/rama-tls-rustls` `src/client/connector_data.rs:10` — `use rustls::client::danger::ServerCertVerifier`
- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::client::danger::ServerCertVerifier`

#### `rustls::*` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rama-tls-rustls-kotlin/rama-tls-rustls` `src/lib.rs:100` — `pub_use rustls::*`

#### `rustls::ALL_VERSIONS` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rama-tls-rustls-kotlin/rama-tls-rustls` `src/server/acceptor_data.rs:10` — `use rustls::ALL_VERSIONS`

#### `rustls::Certificate` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `runfiles-kotlin/pkg_b` `src/lib.rs:1` — `use rustls::Certificate`

#### `rustls::ClientConnection` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tungstenite-kotlin/tungstenite` `src/tls.rs:73` — `use rustls::ClientConnection`

#### `rustls::DigitallySignedStruct` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::DigitallySignedStruct`

#### `rustls::Error` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::Error as TLSError`

#### `rustls::SignatureScheme` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::SignatureScheme`

#### `rustls::client::*` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rama-tls-rustls-kotlin/rama-tls-rustls` `src/lib.rs:109` — `pub_use rustls::client::*`

#### `rustls::client::ClientConfig` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `runfiles-kotlin/pkg_b` `src/lib.rs:1` — `use rustls::client::ClientConfig`

#### `rustls::client::danger::HandshakeSignatureValid` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::client::danger::HandshakeSignatureValid`

#### `rustls::client::danger::ServerCertVerified` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::client::danger::ServerCertVerified`

#### `rustls::crypto::WebPkiSupportedAlgorithms` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::crypto::WebPkiSupportedAlgorithms`

#### `rustls::pki_types` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rama-tls-rustls-kotlin/rama-tls-rustls` `src/type_conversion.rs:11` — `use rustls::pki_types`

#### `rustls::server::*` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rama-tls-rustls-kotlin/rama-tls-rustls` `src/lib.rs:121` — `pub_use rustls::server::*`

#### `rustls::server::ParsedCertificate` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `reqwest-kotlin/reqwest` `src/tls.rs:48` — `use rustls::server::ParsedCertificate`
